name: clockin_photo_app
description: App fotos ClockIn
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '^3.4.0'

dependencies:
  flutter:
    sdk: flutter
  csv: ^6.0.0
  google_mlkit_face_detection: ^0.10.0
  image: ^4.1.7
  image_picker: ^1.0.7
  path_provider: ^2.1.2

flutter:
  uses-material-design: true
